# Plan: Fix skipped speaker note at L156-160

## Root cause

The speaker note at `code-config-curriculum.md:156` uses a YAML literal block scalar (`|`) but has **inconsistent indentation**:

- **Line 157**: `     I confirm...` — **5 spaces** indent
- **Line 159**: `    You see...` — **4 spaces** indent

This causes `serde_yaml` to fail parsing the `<!-- speaker_note: | ... -->` comment command. Presenterm's `should_ignore_comment()` then catches the parse error and, because the comment is multiline (`contains('\n') == true`), silently discards it as a "user comment". Result: the speaker note never renders on the listener terminal.

Compare with the working note at L133-135 which has consistent 4-space indent throughout.

## Fix

1. **Normalise indentation to 4 spaces** on line 157 (remove the 5th space), matching all other speaker notes in the file and standard YAML block scalar convention.

## Verification

1. Run `presenterm code-config-curriculum.md` (publisher)
2. Run `presenterm code-config-curriculum.md --listen-speaker-notes` (listener)
3. Navigate to the Deepseek conversation slide (around `# Previous work` area)
4. Confirm the speaker note `"I confirm I already have all necessary values..."` now appears on the listener

## Files to modify

- `code-config-curriculum.md` — single-line edit at line 157
